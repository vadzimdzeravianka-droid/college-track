# Requirement: [Short Descriptive Title]

## What I Want

We need to support multiple user, right now we have one hardcoded passcode. Instead I want different (for now still hardcoded passcodes? if it is simple we can store it right now and have it hard coded in DB, consider, suggest what is easier, faster, or maybe not to return to it later). Current example we have my some and test account. Iwant separat them by passcode.

## Why

## Constraints

## Examples
for example
passcode `test123` -> shows application for test user - my sendbox
passcode `Arseni123` -> shows real Arseni applicaiton he might start wroking around soon
passcode `AnyOtherPasscode1` -> show any friend Arseni want to share access
